<h1>Recommendations#show</h1>
<%= image_tag image_path("/products/output-#{@recommendation.id}.png") %> 
<p>Message <%= @recommendation.message %> </p>
<p>Recommendation from <%= @sender.username %> </p>

<!-- <container>
<% @recommendation.comments.each do |comment| %>
  <p>Comment from: <%= comment.user.username %></p>
  <p><%= comment.comment %></p>
  <p><%= image_tag comment.style_image.url %></p>
<% end %>
</container> -->

<h3><%=link_to "Shop", @recommendation.product_url, target: '_blank'%></h3>



<section class="benefits">
  <% @recommendation.comments.each do |comment| %>
  <div class="row">
    <div class="small-12 large-4 columns benefit">
      <i class="fa fa-envelope-o"></i>
      <div id="message">
      <h5> From: <%= comment.user.username %> &#x270D; </h5>
      <hr>
      <p><%= comment.comment %></p>
      <p><%= image_tag comment.style_image.url %></p>
      </div>
    </div>
  </div>
  <% end %>
</section>

   



